kind: PersistentVolumeClaim
metadata:
  name: redis-data-redis-app-4
  labels:
    app: redis
spec:
  storageClassName: rook-block
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 200Mb
  selector:
    matchLabels:
      path: nfsshare5
      app: redis
      podindex: "4"

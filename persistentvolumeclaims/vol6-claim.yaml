kind: PersistentVolumeClaim
metadata:
  name: redis-data-redis-app-5
  labels:
    app: redis
spec:
  storageClassName: rook-block
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 200Mb
  selector:
    matchLabels:
      path: nfsshare6
      app: redis
      podindex: "5"
